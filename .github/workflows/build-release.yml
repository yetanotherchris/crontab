name: Build and Create Release

on:
  workflow_dispatch:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            rid: linux-x64
            artifact: crontab
          - os: windows-latest
            rid: win-x64
            artifact: crontab.exe
          - os: macos-latest
            rid: osx-x64
            artifact: crontab
          - os: macos-latest
            rid: osx-arm64
            artifact: crontab

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install GitVersion
        uses: gittools/actions/gitversion/setup@v3
        with:
          versionSpec: '6.2.0'

      - name: Determine Version
        id: gitversion
        uses: gittools/actions/gitversion/execute@v3

      - name: Display GitVersion outputs
        run: |
          echo "SemVer: ${{ steps.gitversion.outputs.semVer }}"

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Restore dependencies
        run: dotnet restore crontab.csproj

      - name: Publish
        run: dotnet publish crontab.csproj -c Release -r ${{ matrix.rid }} -p:Version=${{ steps.gitversion.outputs.semVer }} --self-contained true -p:PublishSingleFile=true -p:PublishTrimmed=true -p:DebugType=None -p:DebugSymbols=false

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.rid }}
          path: bin/Release/net9.0/${{ matrix.rid }}/publish/${{ matrix.artifact }}

  release:
    needs: build
    runs-on: windows-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install GitVersion
        uses: gittools/actions/gitversion/setup@v3
        with:
          versionSpec: '6.2.0'

      - name: Determine Version
        id: gitversion
        uses: gittools/actions/gitversion/execute@v3

      - name: Download linux-x64 artifact
        uses: actions/download-artifact@v4
        with:
          name: linux-x64
          path: ./artifacts/linux-x64

      - name: Download win-x64 artifact
        uses: actions/download-artifact@v4
        with:
          name: win-x64
          path: ./artifacts/win-x64

      - name: Download osx-x64 artifact
        uses: actions/download-artifact@v4
        with:
          name: osx-x64
          path: ./artifacts/osx-x64

      - name: Download osx-arm64 artifact
        uses: actions/download-artifact@v4
        with:
          name: osx-arm64
          path: ./artifacts/osx-arm64

      - name: Rename artifacts with version
        shell: pwsh
        run: |
          $version = "${{ steps.gitversion.outputs.semVer }}"
          Move-Item -Path "./artifacts/linux-x64/crontab" -Destination "./artifacts/crontab-v$version-linux-x64"
          Move-Item -Path "./artifacts/win-x64/crontab.exe" -Destination "./artifacts/crontab-v$version-win-x64.exe"
          Move-Item -Path "./artifacts/osx-x64/crontab" -Destination "./artifacts/crontab-v$version-osx-x64"
          Move-Item -Path "./artifacts/osx-arm64/crontab" -Destination "./artifacts/crontab-v$version-osx-arm64"

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: ./artifacts/crontab-v${{ steps.gitversion.outputs.semVer }}-*
          generate_release_notes: true
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Switch to main branch
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git fetch origin main
          git checkout main

      - name: Update scoop manifest
        shell: pwsh
        run: |
          $version = "${{ steps.gitversion.outputs.semVer }}"
          ./updatescoop.ps1 -Version $version

      - name: Commit and push scoop manifest
        run: |
          git add crontab.json
          git diff --staged --quiet || git commit -m "Update scoop manifest for v${{ steps.gitversion.outputs.semVer }}"
          git push origin main
